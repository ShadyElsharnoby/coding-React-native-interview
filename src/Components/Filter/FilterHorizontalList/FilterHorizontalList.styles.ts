import { StyleSheet } from 'react-native';
import { SH, SW, SF } from '@/src/utils/Scale';
import { COLORS } from '@/src/Theme/Colors';

export default StyleSheet.create({
    contentContainer: {
        backgroundColor: COLORS.primary.blue.b70,
    },
    container: {
        backgroundColor: COLORS.primary.blue.b70,
    },
});
